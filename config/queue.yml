default: &default
  dispatchers:
    - polling_interval: 3
      batch_size: 500
  workers:
    - queues:
        - generation
      threads: 2
      processes: 1
      polling_interval: 3

    - queues:
        - default
        - low_priority
      threads: 2
      processes: 1
      polling_interval: 3

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
