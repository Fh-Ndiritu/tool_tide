<%# app/views/tags/_tag.html.erb %>
<div id="<%= dom_id(tag) %>">
  <span class="inline-flex items-center px-3 py-0.5 rounded-full text-xs font-medium bg-accent text-text-light border border-accent shadow-sm">
    <%= tag.tag_class.humanize %>: <%= tag.title %>
  </span>
</div>
