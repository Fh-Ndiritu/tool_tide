<div class="min-h-screen flex items-center justify-center p-4">
  <div
    class="
      bg-white/10 backdrop-blur-xl p-8 rounded-3xl shadow-2xl max-w-lg w-full transform
      transition-transform duration-300 hover:scale-[1.02] border border-white/20
    "
  >
    <div class="flex flex-col items-center text-center mb-8">
      <svg
        xmlns="http://www.w3.org/2000/svg"
        class="h-16 w-16 text-[#00acc1] mb-4"
        viewBox="0 0 24 24"
        fill="currentColor"
      >
        <path
          d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"
        />
      </svg>

      <h2 class="text-3xl font-extrabold text-white mb-2">
        Keep the <span class="text-[#00acc1]">Creativity</span> Flowing
      </h2>

        You're doing great work. Don't let a low balance pause your progress—top up now to simply keep refining your perfect garden.
    </div>

    <div class="bg-transparent text-white rounded-2xl p-0">
      <hr class="border-t border-white/20 my-8">

      <div class="flex items-center space-x-4 mb-4">
        <%= render(IconComponent.new(name: "sparkles", classes: "h-8 w-8 text-[#00acc1] animate-pulse")) %>
        <h3 class="text-2xl font-bold text-[#00acc1]">
          Maintain Premium Access
        </h3>
      </div>

      <p class="text-lg text-green-400 mt-1 font-bold my-3">
        <b><%= CREDITS_INFO %></b>
      </p>

      <p class="mb-6 text-neutral-300">
        Ensure uninterrupted access to all our high-performance design tools.
      </p>

      <ul class="space-y-4 mb-8">
        <li class="flex items-start space-x-3">
          <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 text-green-400 flex-shrink-0 mt-0.5")) %>
          <span class="font-semibold text-neutral-200">Generate unlimited 4K visualizations</span>
        </li>
        <li class="flex items-start space-x-3">
          <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 text-green-400 flex-shrink-0 mt-0.5")) %>
          <span class="font-semibold text-neutral-200">Refine designs with the <b>AI Mask Brush</b></span>
        </li>
        <li class="flex items-start space-x-3">
          <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 text-green-400 flex-shrink-0 mt-0.5")) %>
          <span class="font-semibold text-neutral-200">
             Instantly create <b>planting plans & shopping lists</b>
          </span>
        </li>
        <li class="flex items-start space-x-3">
          <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 text-green-400 flex-shrink-0 mt-0.5")) %>
          <span class="font-semibold text-neutral-200">Keep your projects stored safely</span>
        </li>
      </ul>

      <div class="flex w-full justify-center">
        <%= button_to "Add Credits & Resume", payment_transactions_path, data: {turbo: false}, method: :post, onclick: "gtag('event', 'begin_checkout', { 'currency': 'USD', 'value': #{DEFAULT_USD_PURCHASE_AMOUNT.to_f} });", class: "m-auto cursor-pointer inline-block bg-[#00acc1] text-black font-bold py-3 px-8 rounded-full shadow-xl transition-all duration-300 transform hover:scale-[1.03] hover:bg-[#26c6da] ring-4 ring-[#00acc1]/30" %>
      </div>
    </div>
  </div>
</div>
