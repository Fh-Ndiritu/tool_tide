<div class="py-6 flex flex-col items-center justify-center space-y-4">
  <div class="relative w-full h-1.5 bg-gray-800 rounded-full overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-r from-purple-600 via-blue-500 to-purple-600 w-1/3 animate-progress-slide rounded-full shadow-[0_0_10px_rgba(147,51,234,0.5)]"></div>
  </div>
  <p class="text-xs font-medium text-gray-400 animate-pulse"><%= message %></p>
</div>

<style>
  @keyframes progress-slide {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(300%); }
  }
  .animate-progress-slide {
    animation: progress-slide 1.5s infinite linear;
  }
</style>
