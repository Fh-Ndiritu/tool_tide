
<%# Set specific content for SEO tags in the layout for the Image Text Extractor page %>

<% content_for(:title, "Image to Text Extractor | OCR from Photos, Notes, Screenshots - Tool Tide") %>

<% content_for(:description, "Easily extract text from any image format: photos, scanned documents, handwritten notes, and screenshots. Tool Tide's free online OCR tool is fast, accurate, and secure for digitizing your content.") %>

<% content_for(:keywords, "image to text, text extractor, OCR, online OCR, convert image to text, free OCR tool, photo to text, handwritten notes to text, screenshot to text, extract text from PDF, JPG to text, PNG to text, Tool Tide") %>
<%# Open Graph (Social Media Sharing) %>
<% content_for(:og_title, "Image to Text Extractor | Free OCR Tool - Tool Tide") %>
<% content_for(:og_description, "Convert photos, notes, and screenshots into editable text online. Tool Tide offers a free, accurate, and fast OCR tool for all your text extraction needs.") %>
<% content_for(:og_image_alt, "Screenshot of Tool Tide's Image to Text Extractor in action, showing text being extracted") %>

<%# Twitter Card (Social Media Sharing) %>
<% content_for(:twitter_title, "Extract Text from Images Free | Tool Tide OCR") %>
<% content_for(:twitter_description, "Need text from a photo or a screenshot? Use Tool Tide's free online OCR tool for quick and accurate text extraction.") %>
<% content_for(:twitter_image_alt, "Visual representation of text being extracted from an image by Tool Tide") %>
<%= content_for(:schema_name, "Image to Text Extractor - Tool Tide") %>
<%= content_for(:schema_description, "Convert images to editable text with Tool Tide's free online OCR tool. Extract text from photos, screenshots, and handwritten notes.") %>

<%# You only need content_for(:robots) if you want to explicitly change it from the default "index, follow" %>
<%# For example, to prevent indexing: %>
<%# content_for(:robots, "noindex, nofollow") %>
<div class="w-full">
  <h1 class="text-4xl font-bold text-center text-text-secondary my-8">
    <%= t("images.titles.from.jpg") %>
    <%= t("images.titles.to.#{@conversion}") %>
  </h1>
 <div class="pb-8">
   <%= turbo_frame_tag "image_form" do %>
      <%= render "image_form", conversion: @conversion, source: @source, image_form: @image_form %>
  <% end %>
 </div>

   <div id="results" class="mt-4 my-4">
    <div class="justify-center items-center">
    </div>
  </div>


  <div class="justify-center items-center card bg-base-100 shadow-xl image-full my-8 p-6 rounded-lg border-dashed border-2 border-gray-300">
    <h2 class="text-2xl font-semibold text-text-secondary"><%= t("images.instructions.convert.title") %> for <%= @image_form.conversion.upcase %></h2>
    <ol class="list-decimal list-inside my-6 text-sm leading-relaxed">
      <% t("images.instructions.convert.steps").each do |step| %>
        <li><%= step %></li>
        <% end %>
    </ol>
  </div>
</div>
