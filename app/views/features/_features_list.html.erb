<%# app/views/features/_features_list.html.erb %>
<%# Local variable: features %>

<%# --- Nudge Paragraph (TOP POSITION) --- %>
<p
  class="
    text-center text-base text-neutral-600 mb-8 p-4 border
    border-accent-secondary rounded-lg bg-accent-secondary/10 shadow-md
    max-w-2xl mx-auto
  "
>
  ✨ <b>Help us build what matters most!</b> Your <b>votes</b> directly
  influence our development priorities. Take a moment to vote on the features
  you want to see launched next.
</p>

<h3
  class="
    font-extrabold text-2xl sm:text-3xl mb-6 text-text-dark border-b
    border-neutral-200 pb-3
  "
>
  Upcoming Features Roadmap
</h3>

<%# --- The Core List Wrapper --- %>
<div id="features" class="space-y-5">
  <% if features.any? %>
    <% features.each do |feature| %>
      <%# Wrap each feature in its DOM ID for Turbo Stream updates (removal/replacement) %>
      <%= turbo_frame_tag feature do %>
        <%= render "features/feature_card", feature: feature %>
      <% end %>
    <% end %>
  <% else %>
    <%# Enhanced Empty State %>
    <div
      class="
        p-10 text-center bg-white rounded-xl border-2
        border-dashed border-neutral-300 shadow-inner
      "
    >
      <p class="text-xl font-semibold text-neutral-600 mb-2">
        No upcoming features are listed for polling yet.
      </p>

      <p class="text-neutral-500 text-base">
        If you're an admin, use the <b>Propose New Feature</b> button to start
        polling the community.
      </p>
    </div>
  <% end %>
</div>
