<%# START: Pricing Section %>

<% content_for :title, "Hadaa Pricing: Advanced AI Design Plans & Free Trial" %>

<% content_for :description, "See Hadaa's subscription plans. Access the intelligent prompt editor, drone views, and plant shopping lists. Start your free, no-commitment trial." %>

<section
  id="pricing"
  class="
    relative py-16 md:py-32 bg-neutral-900 text-white overflow-hidden
    min-h-screen
  "
>
  <%# Background Ambient Glow %>
  <div
    class="
      absolute top-0 left-1/2 -translate-x-1/2 w-full h-[500px] bg-accent/5
      blur-[120px] rounded-full pointer-events-none z-0
    "
  ></div>

  <div class="container mx-auto px-6 max-w-6xl relative z-10">
    <h1
      class="
        text-center text-4xl md:text-5xl font-extrabold mb-6 text-white
        leading-tight
      "
    >
      Simple, Transparent Pricing
    </h1>

    <p
      class="
        text-center text-lg md:text-2xl max-w-3xl mx-auto mb-16
        text-neutral-300 font-light
      "
    >
      Signup and start for free. Experience the magic, then unlock unlimited
      designs with our transparent
      <b class="text-accent">Pay-As-You-Go</b>
      credit system.
    </p>

    <%# START: Pricing Cards Container %>
    <div class="grid md:grid-cols-2 gap-8 max-w-5xl mx-auto">

      <%# START: Free Plan Card %>
      <div
        class="
          p-8 md:p-10 bg-neutral-800/30 backdrop-blur-md rounded-3xl shadow-xl
          border border-white/10 text-white transform transition-all
          duration-500 hover:scale-[1.02] hover:border-white/30
          relative overflow-hidden flex flex-col
        "
      >
        <div class="mb-8">
          <h3 class="text-2xl font-bold mb-2 text-white">Free Plan</h3>
          <div class="flex items-baseline gap-2">
            <span class="text-5xl font-extrabold text-white">Free</span>
          </div>
          <p class="text-neutral-400 mt-2 font-medium">No Credit Card Required</p>
        </div>

        <ul class="space-y-4 mb-8 flex-1 text-neutral-300">
           <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-green-400 flex-shrink-0")) %>
            <span><b class="text-white">60 plus Free Credits</b> to start</span>
          </li>
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-green-400 flex-shrink-0")) %>
            <span>Free Mask Brush Editor</span>
          </li>
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-green-400 flex-shrink-0")) %>
            <span>Free Plants & Localizations</span>
          </li>
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-green-400 flex-shrink-0")) %>
            <span>Free Text Editor</span>
          </li>
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-green-400 flex-shrink-0")) %>
            <span>Free Sketch to 3D</span>
          </li>
        </ul>

          <%= render ButtonComponent.new(
            text: "Start for Free",
            href: new_user_registration_url(host: "hadaa.app"),
            html_options: { rel: "nofollow noreferrer" },
            classes: "w-full bg-white/10 hover:bg-white/20 text-white font-bold py-4 px-8 rounded-full transition duration-300 border border-white/20"
            ) %>
      </div>
      <%# END: Free Plan Card %>

      <%# START: Starter Credit Pack Card %>
      <div
        class="
          p-8 md:p-10 bg-neutral-800/80 backdrop-blur-md rounded-3xl shadow-2xl
          border border-accent text-white transform transition-all
          duration-500 hover:scale-[1.02] hover:shadow-[0_0_40px_rgba(var(--color-accent),0.2)]
          relative overflow-hidden group flex flex-col
        "
      >
         <%# Card Glow Effect %>
        <div
          class="
            absolute -inset-[100%] bg-gradient-to-r from-transparent
            via-white/5 to-transparent skew-x-12 opacity-0
            group-hover:animate-shine pointer-events-none
          "
        ></div>

         <div class="absolute top-0 right-0 bg-accent text-black text-xs font-bold px-3 py-1 rounded-bl-xl">
          MOST POPULAR
        </div>

        <div class="mb-8">
          <h3 class="text-2xl font-bold mb-2 text-accent">Starter Credit Pack</h3>
          <div class="flex items-baseline gap-2">
            <span class="text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-accent to-white">$10</span>
            <span class="text-neutral-400 text-lg">/ once</span>
          </div>
          <p class="text-neutral-400 mt-2 font-medium">For 200 Usage Credits</p>
        </div>

        <ul class="space-y-4 mb-8 flex-1 text-neutral-300">
           <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-accent flex-shrink-0")) %>
            <span><b class="text-white">200 Credits</b> (approx. 40-50 high-res generations)</span>
          </li>
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-accent flex-shrink-0")) %>
            <span>Access to <b class="text-white">exactly all features</b></span>
          </li>
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-accent flex-shrink-0")) %>
            <span>Unlimited High-Resolution Downloads</span>
          </li>
           <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-accent flex-shrink-0")) %>
            <span>Pay-As-You-Go Convenience</span>
          </li>
           <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-accent flex-shrink-0")) %>
            <span>No Subscription Required</span>
          </li>
        </ul>

         <%= render ButtonComponent.new(
            text: "Get 200 Credits",
            href: new_user_registration_url(host: "hadaa.app"),
            html_options: { rel: "nofollow noreferrer" },
            classes: "w-full bg-accent hover:bg-accent-hover text-black font-extrabold py-4 px-8 rounded-full shadow-[0_4px_14px_0_rgba(var(--color-accent),0.39)] hover:shadow-[0_6px_20px_rgba(var(--color-accent),0.23)] hover:-translate-y-0.5 transition duration-300"
            ) %>
      </div>
      <%# END: Starter Credit Pack Card %>

    </div>
    <%# END: Pricing Cards Container %>

    <div class="text-center mt-24 text-white py-16 px-6 border-t border-white/10">
      <h3 class="text-3xl md:text-4xl font-extrabold mb-6">
        Ready to Transform Your Outdoor Space?
      </h3>

      <p
        class="
          text-lg md:text-xl max-w-3xl mx-auto mb-12 leading-relaxed
          text-neutral-400
        "
      >
        Your design journey starts with one simple purchase. Join thousands of
        users who are reimagining their gardens with Hadaa Ai Landscaper.
      </p>

      <%# START: Call to Action Button - Get Credits %>
      <%= render ButtonComponent.new(
        text: "Signup to Get Free credits and start designing",
        href: new_user_registration_url(host: "hadaa.app"),
        html_options: { rel: "nofollow noreferrer" },
        classes: "inline-block bg-white text-black hover:bg-neutral-200 font-extrabold py-4 px-10 rounded-full shadow-lg transition duration-300 ease-in-out transform hover:scale-105"
        ) %>
      <%# END: Call to Action Button - Get Credits %>
    </div>
  </div>
</section>

<%# END: Pricing Section %>
