   <nav class=" p-4 shadow-md rounded-b-lg mb-10 bg-accent" data-controller="navbar">
        <div class="container mx-auto flex justify-between items-center">
            <!-- Logo - Left Aligned -->
            <div class="flex-shrink-0">
                  <%= link_to root_path, class: "flex items-center" do %>
                    <!-- Placeholder image for the logo -->
                    <%= image_tag "tool_tide_logo.jpg",
                    alt: "Tool Tide FYI",
                    class:"h-12 w-12 rounded-full mr-2" %>
                    <% end %>

            </div>

            <!-- Mobile Menu Button (Hamburger) -->
            <div class="md:hidden">
            <%= render ButtonComponent.new(
                  text: "",
                  href: root_path,
                  classes: "bg-transparent hover:bg-secondary focus:outline-none hover:bg-accent text-neutral-900 hover:text-white p-0",
                  svg: 'list',
                  svg_classes: "bg-transparent h-6 w-6 text-neutral-900"
                  ) %>
            </div>

            <!-- Navigation Links - Hidden on mobile, flex on medium and up -->
            <div id="navigation-links" class="hidden md:flex flex-grow justify-end items-center space-x-4">
                <%= render ButtonComponent.new(
                  text: "Home",
                  href: root_path,
                  classes: "#{active_item(root_path)} gap-4 bg-transparent hover:bg-secondary focus:outline-none hover:bg-accent text-neutral-900 hover:text-white p-0 ",
                  ) %>
                <%= render ButtonComponent.new(
                  text: "Text Extractor",
                  href: extract_text_image_path,
                  classes: "#{active_item(extract_text_image_path)} gap-4 bg-transparent hover:bg-secondary focus:outline-none hover:bg-accent text-neutral-900 hover:text-white p-0",
                  ) %>

                  <%= render ButtonComponent.new(
                  text: "Image Converter",
                  href: home_index_path,
                  classes: "#{active_item(home_index_path)} gap-4 bg-transparent hover:bg-secondary focus:outline-none hover:bg-accent text-neutral-900 hover:text-white p-0",
                  ) %>
            </div>
        </div>

        <!-- Mobile Navigation Links (Toggled by Button) -->
        <div id="mobile-menu" class="md:hidden hidden mt-2">
            <a href="/home" class="block text-gray-600 hover:text-blue-600 px-4 py-2 rounded-md transition duration-300 ease-in-out">Home</a>
            <a href="/about" class="block text-gray-600 hover:text-blue-600 px-4 py-2 rounded-md transition duration-300 ease-in-out">About</a>
            <a href="/contact" class="block text-gray-600 hover:text-blue-600 px-4 py-2 rounded-md transition duration-300 ease-in-out">Contact</a>
        </div>
    </nav>
