<div class="flex items-center space-x-2 text-xs py-1">
  <span class="<%= color || 'text-gray-400' %>">●</span>
  <span class="text-gray-300"><%= message %></span>
  <span class="text-gray-600 ml-auto"><%= Time.current.strftime("%H:%M:%S") %></span>
</div>
