<% if execution.image.attached? %>
  <%= image_tag execution.image, class: "w-full h-auto object-cover rounded-lg" %>
<% elsif execution.image_prompt.present? %>
  <div class="text-center w-full py-10">
    <div class="mb-4">
      <svg class="w-16 h-16 mx-auto text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
    </div>
    <%= button_to generate_image_agora_execution_path(execution), method: :post, class: "bg-purple-600 hover:bg-purple-500 text-white font-bold py-2 px-6 rounded-full shadow-lg shadow-purple-900/50 transition-all transform hover:scale-105" do %>
      ✨ Generate Image
    <% end %>
    <p class="mt-4 text-xs text-gray-500 max-w-xs mx-auto">
      Uses Nano Banana via Gemini 2.5 Flash to generate this asset based on the prompt.
    </p>
  </div>
<% else %>
  <div class="text-center py-20 text-gray-600">
    <p>No image prompt available.</p>
  </div>
<% end %>
