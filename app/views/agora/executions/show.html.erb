<%
  root = @execution.post.root
  archetype = root&.content_archetype
  archetype_def = CONTENT_ARCHETYPES.find { |a| a[:type] == archetype } || {}
  persona = root&.persona_context&.with_indifferent_access || {}
%>

<div class="max-w-6xl mx-auto py-8">
  <div class="mb-6 flex justify-between items-center">
    <h1 class="text-3xl font-bold text-white flex items-center">
      <%= link_to agora_executions_path, class: "text-gray-500 hover:text-white mr-4" do %>
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
      <% end %>
      Execution #<%= @execution.id %>
    </h1>
    <div class="flex items-center gap-3">
      <span class="px-3 py-1 bg-purple-900/50 text-purple-300 rounded-full border border-purple-500/30 text-sm font-mono">
        <%= @execution.platform %>
      </span>
      <% if archetype.present? %>
        <span class="px-3 py-1 bg-blue-900/50 text-blue-300 rounded-full border border-blue-500/30 text-sm" title="<%= archetype_def[:goal] %>">
          📋 <%= archetype %>
        </span>
      <% end %>
      <% if persona[:name].present? %>
        <span class="px-3 py-1 bg-amber-900/50 text-amber-300 rounded-full border border-amber-500/30 text-sm" title="<%= persona[:worldview] %>">
          <%= persona[:emoji] %> <%= persona[:name] %>
        </span>
      <% end %>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-5 gap-8 mb-8">
    <!-- LEFT COLUMN: CREATIVE ASSETS -->
    <div class="space-y-6 lg:col-span-2">
      <!-- IMAGE SECTION -->
      <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden shadow-2xl">
        <div class="p-4 border-b border-gray-800 bg-gray-800/50 flex justify-between items-center">
          <h3 class="text-lg font-bold text-white">🎨 Visual Assets</h3>
          <div class="flex gap-2">
             <%= button_to generate_image_agora_execution_path(@execution), method: :post, class: "text-xs bg-purple-900/50 hover:bg-purple-800 border border-purple-500/30 px-3 py-1 rounded text-white transition-colors flex items-center" do %>
               <svg class="w-3 h-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path></svg>
               Regenerate
             <% end %>
          </div>
        </div>

        <%= turbo_stream_from "agora_execution_#{@execution.id}" %>
        <%= render partial: "agora/executions/image_section", locals: { execution: @execution } %>
      </div>

      <!-- VIDEO PROMPT -->
      <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden">
        <div class="p-3 border-b border-gray-800 bg-gray-800/50 flex justify-between items-center">
          <h3 class="text-sm font-bold text-gray-300">🎥 Google Veo Prompt</h3>
          <button data-controller="asset-clipboard" data-action="click->asset-clipboard#copy" data-asset-clipboard-text-param="<%= @execution.video_prompt %>" class="text-xs text-purple-400 hover:text-purple-300">
            📋 Copy
          </button>
        </div>
        <div class="p-4 bg-gray-950">
          <code class="text-xs text-green-400 font-mono block whitespace-pre-wrap"><%= @execution.video_prompt.presence || "No video prompt generated." %></code>
        </div>
      </div>

      <!-- IMAGE PROMPT -->
      <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden">
        <div class="p-3 border-b border-gray-800 bg-gray-800/50 flex justify-between items-center">
          <h3 class="text-sm font-bold text-gray-300">🖼️ Nano Banana Link</h3>
          <button data-controller="asset-clipboard" data-action="click->asset-clipboard#copy" data-asset-clipboard-text-param="<%= @execution.image_prompt %>" class="text-xs text-purple-400 hover:text-purple-300">
            📋 Copy
          </button>
        </div>
        <div class="p-4 bg-gray-950">
          <code class="text-xs text-blue-400 font-mono block whitespace-pre-wrap"><%= @execution.image_prompt.presence || "No image prompt generated." %></code>
        </div>
      </div>
    </div>

    <!-- RIGHT COLUMN: PLATFORM COPY ONLY -->
    <div class="space-y-4 lg:col-span-3">

      <!-- PLATFORM TEXT -->
      <div class="space-y-4">
        <h3 class="text-lg font-bold text-white mb-4">📝 Platform Copy</h3>

        <!-- TikTok Card -->
        <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden group hover:border-pink-500/50 transition-colors duration-300">
          <div class="p-3 border-b border-gray-800 bg-gradient-to-r from-gray-900 to-gray-800 flex justify-between items-center group-hover:from-gray-900 group-hover:to-pink-900/10">
            <div class="flex items-center gap-2">
              <span class="text-lg">🎵</span>
              <span class="text-sm font-bold text-gray-200 group-hover:text-pink-400 transition-colors">TikTok</span>
            </div>
            <button data-controller="asset-clipboard" data-action="click->asset-clipboard#copy" data-asset-clipboard-text-param="<%= strip_markdown(@execution.tiktok_text) %>" class="text-xs bg-gray-800 hover:bg-white hover:text-black border border-gray-700 text-gray-400 px-3 py-1.5 rounded transition-all flex items-center gap-1.5">
              <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 5H6a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2v-1M8 5a2 2 0 002 2h2a2 2 0 002-2M8 5a2 2 0 012-2h2a2 2 0 012 2m0 0h2a2 2 0 012 2v3m2 4H10m0 0l3-3m-3 3l3 3"></path></svg>
              Copy Text
            </button>
          </div>
          <div class="p-4 bg-gray-950/50">
            <% if @execution.tiktok_text.present? %>
              <div class="text-sm text-gray-300 prose prose-invert prose-p:my-2 prose-headings:text-gray-200 prose-headings:mt-4 prose-headings:mb-2 prose-ul:my-2 prose-strong:text-white max-w-none">
                <%= markdown(@execution.tiktok_text, font_class: "text-xs") %>
              </div>
            <% else %>
              <p class="text-sm text-gray-500 italic">No TikTok content generated.</p>
            <% end %>
          </div>
        </div>

        <!-- Facebook Card -->
        <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden group hover:border-blue-500/50 transition-colors duration-300">
          <div class="p-3 border-b border-gray-800 bg-gradient-to-r from-gray-900 to-gray-800 flex justify-between items-center group-hover:from-gray-900 group-hover:to-blue-900/10">
            <div class="flex items-center gap-2">
              <span class="text-lg">📘</span>
              <span class="text-sm font-bold text-gray-200 group-hover:text-blue-400 transition-colors">Facebook</span>
            </div>
            <button data-controller="asset-clipboard" data-action="click->asset-clipboard#copy" data-asset-clipboard-text-param="<%= strip_markdown(@execution.facebook_text) %>" class="text-xs bg-gray-800 hover:bg-white hover:text-black border border-gray-700 text-gray-400 px-3 py-1.5 rounded transition-all flex items-center gap-1.5">
              <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 5H6a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2v-1M8 5a2 2 0 002 2h2a2 2 0 002-2M8 5a2 2 0 012-2h2a2 2 0 012 2m0 0h2a2 2 0 012 2v3m2 4H10m0 0l3-3m-3 3l3 3"></path></svg>
              Copy Text
            </button>
          </div>
          <div class="p-4 bg-gray-950/50">
            <% if @execution.facebook_text.present? %>
              <div class="text-sm text-gray-300 prose prose-invert prose-p:my-2 prose-headings:text-gray-200 prose-headings:mt-4 prose-headings:mb-2 prose-ul:my-2 prose-strong:text-white max-w-none">
                <%= markdown(@execution.facebook_text, font_class: "text-xs") %>
              </div>
            <% else %>
              <p class="text-sm text-gray-500 italic">No Facebook content generated.</p>
            <% end %>
          </div>
        </div>

        <!-- LinkedIn Card -->
        <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden group hover:border-blue-400/50 transition-colors duration-300">
          <div class="p-3 border-b border-gray-800 bg-gradient-to-r from-gray-900 to-gray-800 flex justify-between items-center group-hover:from-gray-900 group-hover:to-blue-800/10">
            <div class="flex items-center gap-2">
              <span class="text-lg">💼</span>
              <span class="text-sm font-bold text-gray-200 group-hover:text-blue-300 transition-colors">LinkedIn</span>
            </div>
            <button data-controller="asset-clipboard" data-action="click->asset-clipboard#copy" data-asset-clipboard-text-param="<%= strip_markdown(@execution.linkedin_text) %>" class="text-xs bg-gray-800 hover:bg-white hover:text-black border border-gray-700 text-gray-400 px-3 py-1.5 rounded transition-all flex items-center gap-1.5">
               <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 5H6a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2v-1M8 5a2 2 0 002 2h2a2 2 0 002-2M8 5a2 2 0 012-2h2a2 2 0 012 2m0 0h2a2 2 0 012 2v3m2 4H10m0 0l3-3m-3 3l3 3"></path></svg>
               Copy Text
            </button>
          </div>
          <div class="p-4 bg-gray-950/50">
            <% if @execution.linkedin_text.present? %>
              <div class="text-sm text-gray-300 prose prose-invert prose-p:my-2 prose-headings:text-gray-200 prose-headings:mt-4 prose-headings:mb-2 prose-ul:my-2 prose-strong:text-white max-w-none">
                <%= markdown(@execution.linkedin_text, font_class: "text-xs") %>
              </div>
            <% else %>
              <p class="text-sm text-gray-500 italic">No LinkedIn content generated.</p>
            <% end %>
          </div>
        </div>

        <!-- Instagram Card -->
        <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden group hover:border-pink-400/50 transition-colors duration-300">
          <div class="p-3 border-b border-gray-800 bg-gradient-to-r from-gray-900 to-gray-800 flex justify-between items-center group-hover:from-gray-900 group-hover:to-pink-800/10">
            <div class="flex items-center gap-2">
              <span class="text-lg">📸</span>
              <span class="text-sm font-bold text-gray-200 group-hover:text-pink-300 transition-colors">Instagram</span>
            </div>
            <button data-controller="asset-clipboard" data-action="click->asset-clipboard#copy" data-asset-clipboard-text-param="<%= strip_markdown(@execution.instagram_text) %>" class="text-xs bg-gray-800 hover:bg-white hover:text-black border border-gray-700 text-gray-400 px-3 py-1.5 rounded transition-all flex items-center gap-1.5">
               <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 5H6a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2v-1M8 5a2 2 0 002 2h2a2 2 0 002-2M8 5a2 2 0 012-2h2a2 2 0 012 2m0 0h2a2 2 0 012 2v3m2 4H10m0 0l3-3m-3 3l3 3"></path></svg>
               Copy Text
            </button>
          </div>
          <div class="p-4 bg-gray-950/50">
            <% if @execution.instagram_text.present? %>
              <div class="text-sm text-gray-300 prose prose-invert prose-p:my-2 prose-headings:text-gray-200 prose-headings:mt-4 prose-headings:mb-2 prose-ul:my-2 prose-strong:text-white max-w-none">
                <%= markdown(@execution.instagram_text, font_class: "text-xs") %>
              </div>
            <% else %>
              <p class="text-sm text-gray-500 italic">No Instagram content generated.</p>
            <% end %>
          </div>
        </div>

        <!-- Pinterest Card -->
        <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden group hover:border-red-400/50 transition-colors duration-300">
          <div class="p-3 border-b border-gray-800 bg-gradient-to-r from-gray-900 to-gray-800 flex justify-between items-center group-hover:from-gray-900 group-hover:to-red-800/10">
            <div class="flex items-center gap-2">
              <span class="text-lg">📌</span>
              <span class="text-sm font-bold text-gray-200 group-hover:text-red-300 transition-colors">Pinterest</span>
            </div>
            <button data-controller="asset-clipboard" data-action="click->asset-clipboard#copy" data-asset-clipboard-text-param="<%= strip_markdown(@execution.pinterest_text) %>" class="text-xs bg-gray-800 hover:bg-white hover:text-black border border-gray-700 text-gray-400 px-3 py-1.5 rounded transition-all flex items-center gap-1.5">
               <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 5H6a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2v-1M8 5a2 2 0 002 2h2a2 2 0 002-2M8 5a2 2 0 012-2h2a2 2 0 012 2m0 0h2a2 2 0 012 2v3m2 4H10m0 0l3-3m-3 3l3 3"></path></svg>
               Copy Text
            </button>
          </div>
          <div class="p-4 bg-gray-950/50">
            <% if @execution.pinterest_text.present? %>
              <div class="text-sm text-gray-300 prose prose-invert prose-p:my-2 prose-headings:text-gray-200 prose-headings:mt-4 prose-headings:mb-2 prose-ul:my-2 prose-strong:text-white max-w-none">
                <%= markdown(@execution.pinterest_text, font_class: "text-xs") %>
              </div>
            <% else %>
              <p class="text-sm text-gray-500 italic">No Pinterest content generated.</p>
            <% end %>
          </div>
        </div>

        <!-- Twitter/X Card -->
        <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden group hover:border-gray-400/50 transition-colors duration-300">
          <div class="p-3 border-b border-gray-800 bg-gradient-to-r from-gray-900 to-gray-800 flex justify-between items-center group-hover:from-gray-900 group-hover:to-gray-700/10">
            <div class="flex items-center gap-2">
              <span class="text-lg">𝕏</span>
              <span class="text-sm font-bold text-gray-200 group-hover:text-white transition-colors">Twitter/X</span>
            </div>
            <button data-controller="asset-clipboard" data-action="click->asset-clipboard#copy" data-asset-clipboard-text-param="<%= strip_markdown(@execution.twitter_text) %>" class="text-xs bg-gray-800 hover:bg-white hover:text-black border border-gray-700 text-gray-400 px-3 py-1.5 rounded transition-all flex items-center gap-1.5">
               <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 5H6a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2v-1M8 5a2 2 0 002 2h2a2 2 0 002-2M8 5a2 2 0 012-2h2a2 2 0 012 2m0 0h2a2 2 0 012 2v3m2 4H10m0 0l3-3m-3 3l3 3"></path></svg>
               Copy Text
            </button>
          </div>
          <div class="p-4 bg-gray-950/50">
            <% if @execution.twitter_text.present? %>
              <div class="text-sm text-gray-300 prose prose-invert prose-p:my-2 prose-headings:text-gray-200 prose-headings:mt-4 prose-headings:mb-2 prose-ul:my-2 prose-strong:text-white max-w-none">
                <%= markdown(@execution.twitter_text, font_class: "text-xs") %>
              </div>
            <% else %>
              <p class="text-sm text-gray-500 italic">No Twitter/X content generated.</p>
            <% end %>
          </div>
        </div>

        <!-- YouTube Card -->
        <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden group hover:border-red-500/50 transition-colors duration-300">
          <div class="p-3 border-b border-gray-800 bg-gradient-to-r from-gray-900 to-gray-800 flex justify-between items-center group-hover:from-gray-900 group-hover:to-red-900/10">
            <div class="flex items-center gap-2">
              <span class="text-lg">▶️</span>
              <span class="text-sm font-bold text-gray-200 group-hover:text-red-400 transition-colors">YouTube</span>
            </div>
            <button data-controller="asset-clipboard" data-action="click->asset-clipboard#copy" data-asset-clipboard-text-param="<%= strip_markdown(@execution.youtube_description) %>" class="text-xs bg-gray-800 hover:bg-white hover:text-black border border-gray-700 text-gray-400 px-3 py-1.5 rounded transition-all flex items-center gap-1.5">
               <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 5H6a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2v-1M8 5a2 2 0 002 2h2a2 2 0 002-2M8 5a2 2 0 012-2h2a2 2 0 012 2m0 0h2a2 2 0 012 2v3m2 4H10m0 0l3-3m-3 3l3 3"></path></svg>
               Copy Text
            </button>
          </div>
          <div class="p-4 bg-gray-950/50">
            <% if @execution.youtube_description.present? %>
              <div class="text-sm text-gray-300 prose prose-invert prose-p:my-2 prose-headings:text-gray-200 prose-headings:mt-4 prose-headings:mb-2 prose-ul:my-2 prose-strong:text-white max-w-none">
                <%= markdown(@execution.youtube_description, font_class: "text-xs") %>
              </div>
            <% else %>
              <p class="text-sm text-gray-500 italic">No YouTube description generated.</p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- FULL WIDTH ROW: ADMIN NOTES -->
  <div class="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden">
    <div class="p-4 border-b border-gray-800 bg-gray-800/50">
      <h3 class="text-lg font-bold text-white">🧠 Strategic Notes</h3>
    </div>
    <div class="p-5 text-sm text-gray-400 prose prose-invert max-w-none">
      <%
        # Parse JSON if it's stored as JSON string (legacy) or display directly
        notes = @execution.admin_notes
        begin
          parsed_notes = JSON.parse(notes)
          display_notes = parsed_notes["admin_notes"] || notes
        rescue
          display_notes = notes
        end
      %>
      <%= simple_format(display_notes) %>
    </div>
  </div>
</div>
