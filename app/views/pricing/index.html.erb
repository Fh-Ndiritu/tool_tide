<%# START: Pricing Section %>

<section id="pricing" class="py-16 md:py-32 bg-white text-gray-900">
  <div class="container mx-auto px-6 max-w-6xl">
    <h2
      class="
        text-center text-4xl md:text-5xl font-extrabold mb-6 text-gray-900
        leading-tight
      "
    >
      Simple, Transparent Pricing
    </h2>

    <p
      class="
        text-center text-lg md:text-2xl max-w-3xl mx-auto mb-16
        text-gray-600 font-light
      "
    >
      Start for free.
      Experience the magic, then unlock unlimited designs with our transparent
      <b>Pay-As-You-Go</b>
      credit system.
    </p>

    <%# START: Pricing Card %>
    <div
      class="
        p-8 md:p-12 mb-8 lg:mb-0 bg-gray-100 rounded-3xl shadow-2xl
        border-4 border-primary text-gray-900 transform transition
        duration-500 hover:scale-[1.03] hover:border-accent hover:shadow-accent/50
      "
    >
      <div class="text-center">
        <h3 class="text-3xl font-bold mb-2 text-primary">
          Starter Credit Pack
        </h3>

        <p class="text-5xl font-extrabold mb-1 leading-none text-accent">
          $10
        </p>

        <p class="text-xl font-bold mb-6 text-gray-600">
          For 200 Usage Credits
        </p>

        <p class="text-lg leading-relaxed mb-8 text-gray-800">
          After your free trial, this one-time purchase unlocks unlimited access to
          all features.
          <b>No subscription. No hidden fees.</b>
          Just pure creative freedom.
        </p>

        <%# START: ButtonComponent - Unlock All Features %>
        <%= render ButtonComponent.new(
            text: "Unlock All Features for $10",
            href: new_user_registration_path,
            classes: "w-auto inline-block bg-accent hover:bg-accent/80 text-black font-extrabold py-4 px-8 rounded-full shadow-lg shadow-accent/50 transition duration-300 transform hover:scale-[1.05]"
            ) %>
        <%# END: ButtonComponent - Unlock All Features %>

        <%# START: Feature List %>
        <ul class="text-left mt-10 space-y-3 text-base text-gray-800">
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-primary flex-shrink-0")) %>
            <span><b>200 Credits</b> to start (e.g., approx. 40-50 high-res generations)</span>
          </li>
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-primary flex-shrink-0")) %>
            Full access to the&nbsp;
            <b>Mask & Ai Editor</b>
          </li>
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-primary flex-shrink-0")) %>
            <b>Unlimited High-Resolution Downloads</b>
          </li>
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-primary flex-shrink-0")) %>
            Location-Aware Plant Recommendations & Shopping Lists
          </li>
          <li class="flex items-start">
            <%= render(IconComponent.new(name: "check", classes: "h-6 w-6 mr-3 mt-0.5 text-primary flex-shrink-0")) %>
            <b>Pay-As-You-Go:</b>
            &nbsp;Purchase more credits at any time.
          </li>
        </ul>

        <%# END: Feature List %>
      </div>
    </div>

    <%# END: Pricing Card %>

    <div class="text-center mt-24 text-gray-900 py-16 px-6 border-t border-gray-300">
      <h3 class="text-3xl md:text-4xl font-extrabold mb-6">
        Ready to Transform Your Outdoor Space?
      </h3>

      <p
        class="
          text-lg md:text-xl max-w-3xl mx-auto mb-10 leading-relaxed
          text-gray-600
        "
      >
        Your design journey starts with one simple purchase. Join thousands of
        users who are reimagining their gardens with Hadaa Ai Landscaper.
      </p>

      <%# START: Call to Action Button - Get Credits %>
      <%= render ButtonComponent.new(
        text: "Get Credits and Start Designing",
        href: new_user_registration_path,
        classes: "mt-4 inline-block bg-accent hover:bg-accent/80 text-black font-extrabold py-4 px-10 rounded-full shadow-lg shadow-accent/50 transition duration-300 ease-in-out transform hover:scale-105"
        ) %>
      <%# END: Call to Action Button - Get Credits %>
    </div>
  </div>
</section>

<%# END: Pricing Section %>
