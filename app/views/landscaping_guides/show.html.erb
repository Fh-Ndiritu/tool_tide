<% content_for :title, @page_title %>
<% content_for :description, @page_description %>

<div>
<% if @blog.html_content.present? %>
  <%= @blog.html_content.gsub('```html', '').gsub('```', '').html_safe %>
<% else %>
  <p>No content available for this blog post.</p>
<% end %>
</div>

<%# CTA Section %>
<div class="bg-green-50 dark:bg-green-900 py-12 mt-12">
  <div class="container mx-auto px-4 text-center">
    <h2 class="text-3xl font-bold mb-4 text-green-900 dark:text-green-100">Ready to Visualize Your Dream Garden?</h2>
    <p class="text-lg text-green-700 dark:text-green-200 mb-8">Try Hadaa's AI Brush and Prompt Editor to bring these ideas to life in your own yard.</p>
    <%= link_to "Start Designing Free", root_path, class: "bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-8 rounded-full shadow-lg transform transition hover:scale-105" %>
  </div>
</div>
