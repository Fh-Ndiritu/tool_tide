<!DOCTYPE html>
<html class="h-full bg-gray-950">
  <head>
    <title>Agora Command Center</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%# Chart.js via CDN for Chartkick stability if importmap fails %>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <style>
      /* Custom Scrollbar for Terminal Feel */
      ::-webkit-scrollbar {
        width: 8px;
        height: 8px;
      }
      ::-webkit-scrollbar-track {
        background: #111827;
      }
      ::-webkit-scrollbar-thumb {
        background: #374151;
        border-radius: 4px;
      }
      ::-webkit-scrollbar-thumb:hover {
        background: #4B5563;
      }
    </style>
  </head>

  <body class="h-full bg-gray-950 text-gray-200 antialiased font-mono">
    <div class="min-h-full flex flex-col">
      <%= render "agora/shared/navbar" %>

      <main class="flex-grow">
        <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
          <%= yield %>
        </div>
      </main>

      <footer class="bg-gray-900 border-t border-gray-800 py-4">
        <div class="max-w-7xl mx-auto px-4 text-center text-xs text-gray-500">
          AGORA NEURAL ENGINE
        </div>
      </footer>
    </div>
  </body>
</html>
