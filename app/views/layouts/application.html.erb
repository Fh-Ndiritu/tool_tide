<!DOCTYPE html>

<html>
  <head>
    <title>
      Try Hadaa AI FREE: Virtual Landscape Planner & Garden Photo Editor - No Credit Card
    </title>

    <meta
      name="description"
      content="Start FREE with Hadaa's AI landscape planner - no credit card required! Try our Mask Designer and AI Editor to visualize seasonal designs, prep your yard for events, and create stunning garden transformations."
    >

    <meta
      property="og:title"
      content="Try Hadaa AI FREE: Virtual Landscape Planner - No Credit Card"
    />

    <meta property="og:type" content="website" />

    <meta
      property="og:url"
      content="<%= request.original_url.split('?').first %>"
    />

    <meta property="og:image" content="<%= asset_path("hadaa.png") %>" />

    <meta
      property="og:description"
      content="Start FREE with Hadaa - no credit card needed! Try our Mask Designer and AI Editor to visualize seasonal garden changes and design your backyard for any occasion."
    />

    <%# Twitter Card (for Twitter/X) %>
    <meta name="twitter:card" content="summary_large_image" />

    <meta name="twitter:site" content="@hadaa_app" />

    <%# Update with your actual Twitter handle %>
    <meta name="twitter:creator" content="@hadaa_app" />

    <%# Update with your actual Twitter handle %>

    <link rel="preconnect" href="https://fonts.googleapis.com">

    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

    <link
      href="https://fonts.googleapis.com/css2?family=Comic+Relief:wght@400;700&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap"
      rel="stylesheet"
    >

    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="<%= asset_path("favicon/favicon-32x32.png") %>"
    >

    <link
      rel="icon"
      type="image/png"
      sizes="16x16"
      href="<%= asset_path("favicon/favicon-16x16.png") %>"
    >

    <meta name="msapplication-TileColor" content="#da532c">

    <%# For Windows tiles %>
    <meta name="theme-color" content="#ffffff">

    <%# <% For browser UI on Android/Chrome %>
    <link rel="shortcut icon" href="<%= asset_path("favicon/favicon.ico") %>">

    <%# Fallback for older browsers, often a multi-res ICO %>
    <%# Dynamic Schema Markup %>
    <% if content_for?(:schema_markup) %>
      <%= yield :schema_markup %>
    <% else %>
      <%# Updated Default Schema Markup for the AI Landscaping Tool %>
      <script type="application/ld+json">
        {
          "@context": "http://schema.org",
          "@type": "SoftwareApplication", <%# Changed to reflect an app/tool %>
          "name": "<%= content_for?(:schema_name) ? yield(:schema_name) : "Hadaa AI Garden Design App" %>",
          "url": "<%= request.original_url.split('?').first %>",
          "operatingSystem": "Web, iOS, Android",
          "applicationCategory": "DesignApplication",
          "aggregateRating": {
            "@type": "AggregateRating",
            "ratingValue": "4.9",
            "reviewCount": "6400"
          },
          "description": "<%= content_for?(:schema_description) ? yield(:schema_description) : "Try Hadaa FREE - no credit card required! Hadaa is an AI-powered virtual landscape design app that lets you transform photos into beautiful gardens, plan seasonal layouts, and prepare for events with our Mask Designer and AI Editor." %>",
          "potentialAction": {
            "@type": "SearchAction",
            "target": "<%= request.base_url %>/search?q={search_term_string}", <%# Adjust with your actual search URL %>
            "query-input": "required name=search_term_string"
          }
        }
      </script>
    <% end %>

    <meta name="viewport" content="width=device-width,initial-scale=1">

    <meta name="apple-mobile-web-app-capable" content="yes">

    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%# = tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <% if content_for?(:head) %>
      <%= yield :head %>
    <% end %>

    <script
      async
      src="https://www.googletagmanager.com/gtag/js?id=G-ESX8FF1NG8"
    ></script>

    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-ESX8FF1NG8');
    </script>

    <script src="https://unpkg.com/konva@9/konva.min.js"></script>

    <%= Sentry.get_trace_propagation_meta.html_safe %>
    <script src="https://t.contentsquare.net/uxa/274e54074137b.js"></script>
  </head>

  <body data-controller="loader">
    <% show_loader = defined?(@mask_request) && @mask_request&.processing? %>
    <%= render partial: "layouts/shared/loader", locals: { record: (show_loader ? @mask_request : nil), klasses: "#{show_loader ? '' : 'hidden'} fixed" } %>
    <%= render "shared/navbar" %>
    <%= render "shared/flash" %>
    <main class="<%= content_for(:main_classes) %>">
      <div class="w-full m-auto block"><%= yield %></div>
    </main>
  </body>
</html>
